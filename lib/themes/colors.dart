import 'package:flutter/material.dart';

/// Centralized app color palette.
class AppColors {
  // Slightly darker grey accent (Tailwind slate-500)
  static const Color accent = Color(0xFF006994); 
  static const Color darkerAccent = Color(0xFF64748B); // Keeping for now, may use later

}